{
  "name": "project-context-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:js": "esbuild assets/js/mobile-menu.js assets/js/character-page.js --bundle --minify --outdir=public/assets/js --format=iife --sourcemap",
    "build:css": "esbuild assets/css/global-v2.css --minify --outfile=public/assets/css/global-v2.css",
    "build": "npm run build:js && npm run build:css"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
