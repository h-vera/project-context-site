{
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js && npx chokidar 'assets/**/*' -c 'node build.js'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "esbuild": "^0.25.9"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
